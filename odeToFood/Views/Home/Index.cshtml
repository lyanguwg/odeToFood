@model IEnumerable<odeToFood.Models.RestaurantListViewModel>
@{
    ViewBag.Title = "Home Page";
}

@foreach(var item in Model)
{
    <div>
        <h4>@item.Name</h4>
        <div>@item.City, @item.Country, @item.CountOfReviews</div>
        <hr />
    </div>
}
